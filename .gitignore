# ── 依赖 ──────────────────────────
node_modules/
.pnpm-store/

# ── 构建产物 ──────────────────────
dist/
build/
*.tsbuildinfo

# ── 环境变量（包含密码/密钥）─────
.env
.env.local
.env.*.local

# ── IDE ───────────────────────────
.idea/
*.swp
*.swo
.vscode/settings.json

# ── OS ────────────────────────────
.DS_Store
Thumbs.db

# ── 日志 ──────────────────────────
*.log
npm-debug.log*
pnpm-debug.log*

# ── 测试覆盖率 ────────────────────
coverage/

# ── Docker 数据卷 ─────────────────
docker/*_data/

# ── 小程序编译产物 ────────────────
apps/worker-mp/dist/

# ── Prisma ────────────────────────
apps/backend/prisma/*.db

# ── 微信小程序私有配置 ─────────────────
project.private.config.json

# ── 设计稿原始文件 ────────────────────
*.png

# ── 微信小程序项目配置（含 AppID）────
project.config.json
